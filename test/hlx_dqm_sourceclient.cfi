  untracked PSet maxEvents = { untracked int32 input = -1 }
  source = EmptySource {
  }

  # name of DQM Source program
  module hlxdqmsource = HLXMonitor{
        untracked string HLXDAQIP = "vmepcs2f17-19"
        untracked uint32 SourcePort = 51001
        untracked string outputFile = "DQM"
        untracked string subSystemName = "HLX"
        untracked string outputDir = "/cms/mon/data/dqm/lumi/root/dqmsource"
        untracked uint32 SavePeriod = 64
        untracked double XMIN = 100
        untracked double XMAX = 3450
        untracked uint32 NBINS = 335 # 10 bunch crossings per bin
        untracked string Style = "BX" # BX for bunch crossing vs. Num events
                                           # History for time vs. Num events
                                           # Dist for Distribution of Num events
        untracked bool Accumulate = true
        untracked uint32 AquireMode = 0  # 0 TCP data, 1 constant fake data
                                         # 2 random data

        untracked bool NewRun_Reset = true
  }
